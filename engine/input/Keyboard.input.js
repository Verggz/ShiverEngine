class Keyboard{
    constructor(){
        this.keys = {};
        this.keydown = window.addEventListener("keydown", function(e){

        });
        this.keyup = winodw.addEventListener("keyup", function(e){
             
        });

        
    }

    GetKeyDown(key){
        if(key){
            
        }
    }

    GetKeyUp(key){

    }

    GetKeyPressed(key){

    }

    GetKeysPressed(keys){

    }
}

module.exports = Keyboard;